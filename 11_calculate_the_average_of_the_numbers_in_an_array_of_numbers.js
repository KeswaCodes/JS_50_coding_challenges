let numbers = [12, 45, 67, 8, 29, 50, 33, 19];
// initialize variable calculating sum in the array
let sum = 0;
// loop through the array
for (number of numbers) {
  sum += number;
}

let average = sum / numbers.length;

console.log(average);
