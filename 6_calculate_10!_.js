// initialize the variable looping through numbers 10 to 1
let factorial = 10;
// initialize variable calculating factorial
let result = 1;

while (factorial > 0) {
  result *= factorial;
  factorial--;
}
console.log(result);
