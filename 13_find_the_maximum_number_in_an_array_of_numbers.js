//function returns the maximum number in the array
function findMax(arr) {
  // loop through array
  for (number of arr) {
    if (number > max) {
      // if the current number in the iteration is greater than the max, set that value to max
      max = number;
    } else {
      continue;
    }
  }
  return max;
}
let numbers = [10, 5, 22, -3, 17, 0, -8, 12.5];
let max = 0;

let result = findMax(numbers);
console.log(result);
